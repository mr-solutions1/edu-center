{
  "name": "rakan-institute-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Rakan Academy Monorepo - Enterprise ERP",
  "scripts": {
    "prepare": "husky"
  },
  "dependencies": {},
  "lint-staged": {
    "edu-core-api/src/**/*.js": [
      "cd edu-core-api && npx prettier --write",
      "cd edu-core-api && npx eslint --fix"
    ],
    "edu-core-web/src/**/*.{js,jsx}": [
      "cd edu-core-web && npx prettier --write",
      "cd edu-core-web && npx eslint --fix"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8"
  }
}
